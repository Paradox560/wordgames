import { ReactNode } from "react";
export default function GameShell({index,title,description,children}:{index:string;title:string;description:string;children:ReactNode}){return <main className="game-page"><div className="game-layout"><aside className="game-intro"><p className="eyebrow">Workbench {index}</p><h1 className="game-title">{title}</h1><p className="game-copy">{description}</p><div className="step-list"><p><span>1</span>Enter the puzzle clues</p><p><span>2</span>Check the board once</p><p><span>3</span>Find your missing word</p></div></aside><section className="workbench"><div className="workbench-head"><span className="text-xs font-bold uppercase tracking-[.13em]">Puzzle board</span><span className="font-mono text-xs text-[#68685f]">{index} / 06</span></div><div className="workbench-body">{children}</div></section></div></main>}
